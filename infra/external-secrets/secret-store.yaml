apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: aws-secrets-manager
  namespace: aqtms
spec:
  provider:
    aws:
      service: SecretsManager
      region: ap-east-1  # Change to your region
      auth:
        jwt:
          serviceAccountRef:
            name: external-secrets-sa
